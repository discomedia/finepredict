# Shared local and production values. Keep one master .env locally and copy the
# applicable values to Railway and Netlify through their CLIs.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
OPENAI_API_KEY=
ADMIN_API_KEY=
PORT=3001
WEB_ORIGIN=http://localhost:5173
VITE_API_BASE_URL=http://localhost:3001
